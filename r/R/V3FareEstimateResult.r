# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' V3FareEstimateResult Class
#'
#' @field IsEarlyBird 
#' @field IsJourneyInFreeTramZone 
#' @field IsThisWeekendJourney 
#' @field ZoneInfo 
#' @field PassengerFares 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V3FareEstimateResult <- R6::R6Class(
  'V3FareEstimateResult',
  public = list(
    `IsEarlyBird` = NULL,
    `IsJourneyInFreeTramZone` = NULL,
    `IsThisWeekendJourney` = NULL,
    `ZoneInfo` = NULL,
    `PassengerFares` = NULL,
    initialize = function(`IsEarlyBird`, `IsJourneyInFreeTramZone`, `IsThisWeekendJourney`, `ZoneInfo`, `PassengerFares`){
      if (!missing(`IsEarlyBird`)) {
        self$`IsEarlyBird` <- `IsEarlyBird`
      }
      if (!missing(`IsJourneyInFreeTramZone`)) {
        self$`IsJourneyInFreeTramZone` <- `IsJourneyInFreeTramZone`
      }
      if (!missing(`IsThisWeekendJourney`)) {
        self$`IsThisWeekendJourney` <- `IsThisWeekendJourney`
      }
      if (!missing(`ZoneInfo`)) {
        stopifnot(R6::is.R6(`ZoneInfo`))
        self$`ZoneInfo` <- `ZoneInfo`
      }
      if (!missing(`PassengerFares`)) {
        stopifnot(is.list(`PassengerFares`), length(`PassengerFares`) != 0)
        lapply(`PassengerFares`, function(x) stopifnot(R6::is.R6(x)))
        self$`PassengerFares` <- `PassengerFares`
      }
    },
    toJSON = function() {
      V3FareEstimateResultObject <- list()
      if (!is.null(self$`IsEarlyBird`)) {
        V3FareEstimateResultObject[['IsEarlyBird']] <- self$`IsEarlyBird`
      }
      if (!is.null(self$`IsJourneyInFreeTramZone`)) {
        V3FareEstimateResultObject[['IsJourneyInFreeTramZone']] <- self$`IsJourneyInFreeTramZone`
      }
      if (!is.null(self$`IsThisWeekendJourney`)) {
        V3FareEstimateResultObject[['IsThisWeekendJourney']] <- self$`IsThisWeekendJourney`
      }
      if (!is.null(self$`ZoneInfo`)) {
        V3FareEstimateResultObject[['ZoneInfo']] <- self$`ZoneInfo`$toJSON()
      }
      if (!is.null(self$`PassengerFares`)) {
        V3FareEstimateResultObject[['PassengerFares']] <- lapply(self$`PassengerFares`, function(x) x$toJSON())
      }

      V3FareEstimateResultObject
    },
    fromJSON = function(V3FareEstimateResultJson) {
      V3FareEstimateResultObject <- jsonlite::fromJSON(V3FareEstimateResultJson)
      if (!is.null(V3FareEstimateResultObject$`IsEarlyBird`)) {
        self$`IsEarlyBird` <- V3FareEstimateResultObject$`IsEarlyBird`
      }
      if (!is.null(V3FareEstimateResultObject$`IsJourneyInFreeTramZone`)) {
        self$`IsJourneyInFreeTramZone` <- V3FareEstimateResultObject$`IsJourneyInFreeTramZone`
      }
      if (!is.null(V3FareEstimateResultObject$`IsThisWeekendJourney`)) {
        self$`IsThisWeekendJourney` <- V3FareEstimateResultObject$`IsThisWeekendJourney`
      }
      if (!is.null(V3FareEstimateResultObject$`ZoneInfo`)) {
        ZoneInfoObject <- V3ZoneInfo$new()
        ZoneInfoObject$fromJSON(jsonlite::toJSON(V3FareEstimateResultObject$ZoneInfo, auto_unbox = TRUE))
        self$`ZoneInfo` <- ZoneInfoObject
      }
      if (!is.null(V3FareEstimateResultObject$`PassengerFares`)) {
        self$`PassengerFares` <- lapply(V3FareEstimateResultObject$`PassengerFares`, function(x) {
          PassengerFaresObject <- V3PassengerFare$new()
          PassengerFaresObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          PassengerFaresObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "IsEarlyBird": %s,
           "IsJourneyInFreeTramZone": %s,
           "IsThisWeekendJourney": %s,
           "ZoneInfo": %s,
           "PassengerFares": [%s]
        }',
        self$`IsEarlyBird`,
        self$`IsJourneyInFreeTramZone`,
        self$`IsThisWeekendJourney`,
        self$`ZoneInfo`$toJSON(),
        lapply(self$`PassengerFares`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(V3FareEstimateResultJson) {
      V3FareEstimateResultObject <- jsonlite::fromJSON(V3FareEstimateResultJson)
      self$`IsEarlyBird` <- V3FareEstimateResultObject$`IsEarlyBird`
      self$`IsJourneyInFreeTramZone` <- V3FareEstimateResultObject$`IsJourneyInFreeTramZone`
      self$`IsThisWeekendJourney` <- V3FareEstimateResultObject$`IsThisWeekendJourney`
      V3ZoneInfoObject <- V3ZoneInfo$new()
      self$`ZoneInfo` <- V3ZoneInfoObject$fromJSON(jsonlite::toJSON(V3FareEstimateResultObject$ZoneInfo, auto_unbox = TRUE))
      self$`PassengerFares` <- lapply(V3FareEstimateResultObject$`PassengerFares`, function(x) V3PassengerFare$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
