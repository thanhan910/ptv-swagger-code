# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' V3DynamoDbTimetable Class
#'
#' @field table_name 
#' @field parser_version 
#' @field parser_mapping_version 
#' @field pt_version 
#' @field pt_mapping_version 
#' @field transport_type 
#' @field applicable_local_date 
#' @field exists 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V3DynamoDbTimetable <- R6::R6Class(
  'V3DynamoDbTimetable',
  public = list(
    `table_name` = NULL,
    `parser_version` = NULL,
    `parser_mapping_version` = NULL,
    `pt_version` = NULL,
    `pt_mapping_version` = NULL,
    `transport_type` = NULL,
    `applicable_local_date` = NULL,
    `exists` = NULL,
    initialize = function(`table_name`, `parser_version`, `parser_mapping_version`, `pt_version`, `pt_mapping_version`, `transport_type`, `applicable_local_date`, `exists`){
      if (!missing(`table_name`)) {
        stopifnot(is.character(`table_name`), length(`table_name`) == 1)
        self$`table_name` <- `table_name`
      }
      if (!missing(`parser_version`)) {
        stopifnot(is.numeric(`parser_version`), length(`parser_version`) == 1)
        self$`parser_version` <- `parser_version`
      }
      if (!missing(`parser_mapping_version`)) {
        stopifnot(is.character(`parser_mapping_version`), length(`parser_mapping_version`) == 1)
        self$`parser_mapping_version` <- `parser_mapping_version`
      }
      if (!missing(`pt_version`)) {
        stopifnot(is.numeric(`pt_version`), length(`pt_version`) == 1)
        self$`pt_version` <- `pt_version`
      }
      if (!missing(`pt_mapping_version`)) {
        stopifnot(is.character(`pt_mapping_version`), length(`pt_mapping_version`) == 1)
        self$`pt_mapping_version` <- `pt_mapping_version`
      }
      if (!missing(`transport_type`)) {
        stopifnot(is.numeric(`transport_type`), length(`transport_type`) == 1)
        self$`transport_type` <- `transport_type`
      }
      if (!missing(`applicable_local_date`)) {
        stopifnot(is.character(`applicable_local_date`), length(`applicable_local_date`) == 1)
        self$`applicable_local_date` <- `applicable_local_date`
      }
      if (!missing(`exists`)) {
        self$`exists` <- `exists`
      }
    },
    toJSON = function() {
      V3DynamoDbTimetableObject <- list()
      if (!is.null(self$`table_name`)) {
        V3DynamoDbTimetableObject[['table_name']] <- self$`table_name`
      }
      if (!is.null(self$`parser_version`)) {
        V3DynamoDbTimetableObject[['parser_version']] <- self$`parser_version`
      }
      if (!is.null(self$`parser_mapping_version`)) {
        V3DynamoDbTimetableObject[['parser_mapping_version']] <- self$`parser_mapping_version`
      }
      if (!is.null(self$`pt_version`)) {
        V3DynamoDbTimetableObject[['pt_version']] <- self$`pt_version`
      }
      if (!is.null(self$`pt_mapping_version`)) {
        V3DynamoDbTimetableObject[['pt_mapping_version']] <- self$`pt_mapping_version`
      }
      if (!is.null(self$`transport_type`)) {
        V3DynamoDbTimetableObject[['transport_type']] <- self$`transport_type`
      }
      if (!is.null(self$`applicable_local_date`)) {
        V3DynamoDbTimetableObject[['applicable_local_date']] <- self$`applicable_local_date`
      }
      if (!is.null(self$`exists`)) {
        V3DynamoDbTimetableObject[['exists']] <- self$`exists`
      }

      V3DynamoDbTimetableObject
    },
    fromJSON = function(V3DynamoDbTimetableJson) {
      V3DynamoDbTimetableObject <- jsonlite::fromJSON(V3DynamoDbTimetableJson)
      if (!is.null(V3DynamoDbTimetableObject$`table_name`)) {
        self$`table_name` <- V3DynamoDbTimetableObject$`table_name`
      }
      if (!is.null(V3DynamoDbTimetableObject$`parser_version`)) {
        self$`parser_version` <- V3DynamoDbTimetableObject$`parser_version`
      }
      if (!is.null(V3DynamoDbTimetableObject$`parser_mapping_version`)) {
        self$`parser_mapping_version` <- V3DynamoDbTimetableObject$`parser_mapping_version`
      }
      if (!is.null(V3DynamoDbTimetableObject$`pt_version`)) {
        self$`pt_version` <- V3DynamoDbTimetableObject$`pt_version`
      }
      if (!is.null(V3DynamoDbTimetableObject$`pt_mapping_version`)) {
        self$`pt_mapping_version` <- V3DynamoDbTimetableObject$`pt_mapping_version`
      }
      if (!is.null(V3DynamoDbTimetableObject$`transport_type`)) {
        self$`transport_type` <- V3DynamoDbTimetableObject$`transport_type`
      }
      if (!is.null(V3DynamoDbTimetableObject$`applicable_local_date`)) {
        self$`applicable_local_date` <- V3DynamoDbTimetableObject$`applicable_local_date`
      }
      if (!is.null(V3DynamoDbTimetableObject$`exists`)) {
        self$`exists` <- V3DynamoDbTimetableObject$`exists`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "table_name": %s,
           "parser_version": %d,
           "parser_mapping_version": %s,
           "pt_version": %d,
           "pt_mapping_version": %s,
           "transport_type": %d,
           "applicable_local_date": %s,
           "exists": %s
        }',
        self$`table_name`,
        self$`parser_version`,
        self$`parser_mapping_version`,
        self$`pt_version`,
        self$`pt_mapping_version`,
        self$`transport_type`,
        self$`applicable_local_date`,
        self$`exists`
      )
    },
    fromJSONString = function(V3DynamoDbTimetableJson) {
      V3DynamoDbTimetableObject <- jsonlite::fromJSON(V3DynamoDbTimetableJson)
      self$`table_name` <- V3DynamoDbTimetableObject$`table_name`
      self$`parser_version` <- V3DynamoDbTimetableObject$`parser_version`
      self$`parser_mapping_version` <- V3DynamoDbTimetableObject$`parser_mapping_version`
      self$`pt_version` <- V3DynamoDbTimetableObject$`pt_version`
      self$`pt_mapping_version` <- V3DynamoDbTimetableObject$`pt_mapping_version`
      self$`transport_type` <- V3DynamoDbTimetableObject$`transport_type`
      self$`applicable_local_date` <- V3DynamoDbTimetableObject$`applicable_local_date`
      self$`exists` <- V3DynamoDbTimetableObject$`exists`
    }
  )
)
