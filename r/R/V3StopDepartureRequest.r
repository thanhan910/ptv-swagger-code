# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' V3StopDepartureRequest Class
#'
#' @field route_type 
#' @field stop_id 
#' @field max_results 
#' @field gtfs 
#' @field route_directions 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V3StopDepartureRequest <- R6::R6Class(
  'V3StopDepartureRequest',
  public = list(
    `route_type` = NULL,
    `stop_id` = NULL,
    `max_results` = NULL,
    `gtfs` = NULL,
    `route_directions` = NULL,
    initialize = function(`route_type`, `stop_id`, `max_results`, `gtfs`, `route_directions`){
      if (!missing(`route_type`)) {
        stopifnot(is.numeric(`route_type`), length(`route_type`) == 1)
        self$`route_type` <- `route_type`
      }
      if (!missing(`stop_id`)) {
        stopifnot(is.numeric(`stop_id`), length(`stop_id`) == 1)
        self$`stop_id` <- `stop_id`
      }
      if (!missing(`max_results`)) {
        stopifnot(is.numeric(`max_results`), length(`max_results`) == 1)
        self$`max_results` <- `max_results`
      }
      if (!missing(`gtfs`)) {
        self$`gtfs` <- `gtfs`
      }
      if (!missing(`route_directions`)) {
        stopifnot(is.list(`route_directions`), length(`route_directions`) != 0)
        lapply(`route_directions`, function(x) stopifnot(R6::is.R6(x)))
        self$`route_directions` <- `route_directions`
      }
    },
    toJSON = function() {
      V3StopDepartureRequestObject <- list()
      if (!is.null(self$`route_type`)) {
        V3StopDepartureRequestObject[['route_type']] <- self$`route_type`
      }
      if (!is.null(self$`stop_id`)) {
        V3StopDepartureRequestObject[['stop_id']] <- self$`stop_id`
      }
      if (!is.null(self$`max_results`)) {
        V3StopDepartureRequestObject[['max_results']] <- self$`max_results`
      }
      if (!is.null(self$`gtfs`)) {
        V3StopDepartureRequestObject[['gtfs']] <- self$`gtfs`
      }
      if (!is.null(self$`route_directions`)) {
        V3StopDepartureRequestObject[['route_directions']] <- lapply(self$`route_directions`, function(x) x$toJSON())
      }

      V3StopDepartureRequestObject
    },
    fromJSON = function(V3StopDepartureRequestJson) {
      V3StopDepartureRequestObject <- jsonlite::fromJSON(V3StopDepartureRequestJson)
      if (!is.null(V3StopDepartureRequestObject$`route_type`)) {
        self$`route_type` <- V3StopDepartureRequestObject$`route_type`
      }
      if (!is.null(V3StopDepartureRequestObject$`stop_id`)) {
        self$`stop_id` <- V3StopDepartureRequestObject$`stop_id`
      }
      if (!is.null(V3StopDepartureRequestObject$`max_results`)) {
        self$`max_results` <- V3StopDepartureRequestObject$`max_results`
      }
      if (!is.null(V3StopDepartureRequestObject$`gtfs`)) {
        self$`gtfs` <- V3StopDepartureRequestObject$`gtfs`
      }
      if (!is.null(V3StopDepartureRequestObject$`route_directions`)) {
        self$`route_directions` <- lapply(V3StopDepartureRequestObject$`route_directions`, function(x) {
          route_directionsObject <- V3StopDepartureRequestRouteDirection$new()
          route_directionsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          route_directionsObject
        })
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "route_type": %d,
           "stop_id": %d,
           "max_results": %d,
           "gtfs": %s,
           "route_directions": [%s]
        }',
        self$`route_type`,
        self$`stop_id`,
        self$`max_results`,
        self$`gtfs`,
        lapply(self$`route_directions`, function(x) paste(x$toJSON(), sep=","))
      )
    },
    fromJSONString = function(V3StopDepartureRequestJson) {
      V3StopDepartureRequestObject <- jsonlite::fromJSON(V3StopDepartureRequestJson)
      self$`route_type` <- V3StopDepartureRequestObject$`route_type`
      self$`stop_id` <- V3StopDepartureRequestObject$`stop_id`
      self$`max_results` <- V3StopDepartureRequestObject$`max_results`
      self$`gtfs` <- V3StopDepartureRequestObject$`gtfs`
      self$`route_directions` <- lapply(V3StopDepartureRequestObject$`route_directions`, function(x) V3StopDepartureRequestRouteDirection$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
    }
  )
)
