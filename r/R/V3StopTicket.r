# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' V3StopTicket Class
#'
#' @field ticket_type 
#' @field zone 
#' @field is_free_fare_zone 
#' @field ticket_machine 
#' @field ticket_checks 
#' @field vline_reservation 
#' @field ticket_zones 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V3StopTicket <- R6::R6Class(
  'V3StopTicket',
  public = list(
    `ticket_type` = NULL,
    `zone` = NULL,
    `is_free_fare_zone` = NULL,
    `ticket_machine` = NULL,
    `ticket_checks` = NULL,
    `vline_reservation` = NULL,
    `ticket_zones` = NULL,
    initialize = function(`ticket_type`, `zone`, `is_free_fare_zone`, `ticket_machine`, `ticket_checks`, `vline_reservation`, `ticket_zones`){
      if (!missing(`ticket_type`)) {
        stopifnot(is.character(`ticket_type`), length(`ticket_type`) == 1)
        self$`ticket_type` <- `ticket_type`
      }
      if (!missing(`zone`)) {
        stopifnot(is.character(`zone`), length(`zone`) == 1)
        self$`zone` <- `zone`
      }
      if (!missing(`is_free_fare_zone`)) {
        self$`is_free_fare_zone` <- `is_free_fare_zone`
      }
      if (!missing(`ticket_machine`)) {
        self$`ticket_machine` <- `ticket_machine`
      }
      if (!missing(`ticket_checks`)) {
        self$`ticket_checks` <- `ticket_checks`
      }
      if (!missing(`vline_reservation`)) {
        self$`vline_reservation` <- `vline_reservation`
      }
      if (!missing(`ticket_zones`)) {
        stopifnot(is.list(`ticket_zones`), length(`ticket_zones`) != 0)
        lapply(`ticket_zones`, function(x) stopifnot(is.character(x)))
        self$`ticket_zones` <- `ticket_zones`
      }
    },
    toJSON = function() {
      V3StopTicketObject <- list()
      if (!is.null(self$`ticket_type`)) {
        V3StopTicketObject[['ticket_type']] <- self$`ticket_type`
      }
      if (!is.null(self$`zone`)) {
        V3StopTicketObject[['zone']] <- self$`zone`
      }
      if (!is.null(self$`is_free_fare_zone`)) {
        V3StopTicketObject[['is_free_fare_zone']] <- self$`is_free_fare_zone`
      }
      if (!is.null(self$`ticket_machine`)) {
        V3StopTicketObject[['ticket_machine']] <- self$`ticket_machine`
      }
      if (!is.null(self$`ticket_checks`)) {
        V3StopTicketObject[['ticket_checks']] <- self$`ticket_checks`
      }
      if (!is.null(self$`vline_reservation`)) {
        V3StopTicketObject[['vline_reservation']] <- self$`vline_reservation`
      }
      if (!is.null(self$`ticket_zones`)) {
        V3StopTicketObject[['ticket_zones']] <- self$`ticket_zones`
      }

      V3StopTicketObject
    },
    fromJSON = function(V3StopTicketJson) {
      V3StopTicketObject <- jsonlite::fromJSON(V3StopTicketJson)
      if (!is.null(V3StopTicketObject$`ticket_type`)) {
        self$`ticket_type` <- V3StopTicketObject$`ticket_type`
      }
      if (!is.null(V3StopTicketObject$`zone`)) {
        self$`zone` <- V3StopTicketObject$`zone`
      }
      if (!is.null(V3StopTicketObject$`is_free_fare_zone`)) {
        self$`is_free_fare_zone` <- V3StopTicketObject$`is_free_fare_zone`
      }
      if (!is.null(V3StopTicketObject$`ticket_machine`)) {
        self$`ticket_machine` <- V3StopTicketObject$`ticket_machine`
      }
      if (!is.null(V3StopTicketObject$`ticket_checks`)) {
        self$`ticket_checks` <- V3StopTicketObject$`ticket_checks`
      }
      if (!is.null(V3StopTicketObject$`vline_reservation`)) {
        self$`vline_reservation` <- V3StopTicketObject$`vline_reservation`
      }
      if (!is.null(V3StopTicketObject$`ticket_zones`)) {
        self$`ticket_zones` <- V3StopTicketObject$`ticket_zones`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "ticket_type": %s,
           "zone": %s,
           "is_free_fare_zone": %s,
           "ticket_machine": %s,
           "ticket_checks": %s,
           "vline_reservation": %s,
           "ticket_zones": [%s]
        }',
        self$`ticket_type`,
        self$`zone`,
        self$`is_free_fare_zone`,
        self$`ticket_machine`,
        self$`ticket_checks`,
        self$`vline_reservation`,
        lapply(self$`ticket_zones`, function(x) paste(paste0('"', x, '"'), sep=","))
      )
    },
    fromJSONString = function(V3StopTicketJson) {
      V3StopTicketObject <- jsonlite::fromJSON(V3StopTicketJson)
      self$`ticket_type` <- V3StopTicketObject$`ticket_type`
      self$`zone` <- V3StopTicketObject$`zone`
      self$`is_free_fare_zone` <- V3StopTicketObject$`is_free_fare_zone`
      self$`ticket_machine` <- V3StopTicketObject$`ticket_machine`
      self$`ticket_checks` <- V3StopTicketObject$`ticket_checks`
      self$`vline_reservation` <- V3StopTicketObject$`vline_reservation`
      self$`ticket_zones` <- V3StopTicketObject$`ticket_zones`
    }
  )
)
