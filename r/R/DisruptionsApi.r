# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git
#' @title Disruptions operations
#' @description swagger.Disruptions
#'
#' @field path Stores url path of the request.
#' @field apiClient Handles the client-server communication.
#' @field userAgent Set the user agent of the request.
#'
#' @importFrom R6 R6Class
#'
#' @section Methods:
#' \describe{
#'
#' disruptions_get_all_disruptions View all disruptions for all route types
#'
#'
#' disruptions_get_disruption_by_id View a specific disruption
#'
#'
#' disruptions_get_disruption_modes Get all disruption modes
#'
#'
#' disruptions_get_disruptions_by_route View all disruptions for a particular route
#'
#'
#' disruptions_get_disruptions_by_route_and_stop View all disruptions for a particular route and stop
#'
#'
#' disruptions_get_disruptions_by_stop View all disruptions for a particular stop
#'
#' }
#'
#' @export
DisruptionsApi <- R6::R6Class(
  'DisruptionsApi',
  public = list(
    userAgent = "Swagger-Codegen/1.0.0/r",
    apiClient = NULL,
    initialize = function(apiClient){
      if (!missing(apiClient)) {
        self$apiClient <- apiClient
      }
      else {
        self$apiClient <- ApiClient$new()
      }
    },
    disruptions_get_all_disruptions = function(route_types, disruption_modes, disruption_status, token, devid, signature, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`route_types`)) {
        queryParams['route_types'] <- route_types
      }

      if (!missing(`disruption_modes`)) {
        queryParams['disruption_modes'] <- disruption_modes
      }

      if (!missing(`disruption_status`)) {
        queryParams['disruption_status'] <- disruption_status
      }

      if (!missing(`token`)) {
        queryParams['token'] <- token
      }

      if (!missing(`devid`)) {
        queryParams['devid'] <- devid
      }

      if (!missing(`signature`)) {
        queryParams['signature'] <- signature
      }

      urlPath <- "/v3/disruptions"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- V3DisruptionsResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    disruptions_get_disruption_by_id = function(disruption_id, token, devid, signature, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`token`)) {
        queryParams['token'] <- token
      }

      if (!missing(`devid`)) {
        queryParams['devid'] <- devid
      }

      if (!missing(`signature`)) {
        queryParams['signature'] <- signature
      }

      urlPath <- "/v3/disruptions/{disruption_id}"
      if (!missing(`disruption_id`)) {
        urlPath <- gsub(paste0("\\{", "disruption_id", "\\}"), `disruption_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- V3DisruptionResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    disruptions_get_disruption_modes = function(token, devid, signature, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`token`)) {
        queryParams['token'] <- token
      }

      if (!missing(`devid`)) {
        queryParams['devid'] <- devid
      }

      if (!missing(`signature`)) {
        queryParams['signature'] <- signature
      }

      urlPath <- "/v3/disruptions/modes"
      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- V3DisruptionModesResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    disruptions_get_disruptions_by_route = function(route_id, disruption_status, token, devid, signature, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`disruption_status`)) {
        queryParams['disruption_status'] <- disruption_status
      }

      if (!missing(`token`)) {
        queryParams['token'] <- token
      }

      if (!missing(`devid`)) {
        queryParams['devid'] <- devid
      }

      if (!missing(`signature`)) {
        queryParams['signature'] <- signature
      }

      urlPath <- "/v3/disruptions/route/{route_id}"
      if (!missing(`route_id`)) {
        urlPath <- gsub(paste0("\\{", "route_id", "\\}"), `route_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- V3DisruptionsResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    disruptions_get_disruptions_by_route_and_stop = function(route_id, stop_id, disruption_status, token, devid, signature, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`disruption_status`)) {
        queryParams['disruption_status'] <- disruption_status
      }

      if (!missing(`token`)) {
        queryParams['token'] <- token
      }

      if (!missing(`devid`)) {
        queryParams['devid'] <- devid
      }

      if (!missing(`signature`)) {
        queryParams['signature'] <- signature
      }

      urlPath <- "/v3/disruptions/route/{route_id}/stop/{stop_id}"
      if (!missing(`route_id`)) {
        urlPath <- gsub(paste0("\\{", "route_id", "\\}"), `route_id`, urlPath)
      }

      if (!missing(`stop_id`)) {
        urlPath <- gsub(paste0("\\{", "stop_id", "\\}"), `stop_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- V3DisruptionsResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
    disruptions_get_disruptions_by_stop = function(stop_id, disruption_status, token, devid, signature, ...){
      args <- list(...)
      queryParams <- list()
      headerParams <- character()

      if (!missing(`disruption_status`)) {
        queryParams['disruption_status'] <- disruption_status
      }

      if (!missing(`token`)) {
        queryParams['token'] <- token
      }

      if (!missing(`devid`)) {
        queryParams['devid'] <- devid
      }

      if (!missing(`signature`)) {
        queryParams['signature'] <- signature
      }

      urlPath <- "/v3/disruptions/stop/{stop_id}"
      if (!missing(`stop_id`)) {
        urlPath <- gsub(paste0("\\{", "stop_id", "\\}"), `stop_id`, urlPath)
      }

      resp <- self$apiClient$callApi(url = paste0(self$apiClient$basePath, urlPath),
                                 method = "GET",
                                 queryParams = queryParams,
                                 headerParams = headerParams,
                                 body = body,
                                 ...)
      
      if (httr::status_code(resp) >= 200 && httr::status_code(resp) <= 299) {
        returnObject <- V3DisruptionsResponse$new()
        result <- returnObject$fromJSON(httr::content(resp, "text", encoding = "UTF-8"))
        Response$new(returnObject, resp)
      } else if (httr::status_code(resp) >= 400 && httr::status_code(resp) <= 499) {
        Response$new("API client error", resp)
      } else if (httr::status_code(resp) >= 500 && httr::status_code(resp) <= 599) {
        Response$new("API server error", resp)
      }

    }
  )
)
