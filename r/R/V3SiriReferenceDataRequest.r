# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' V3SiriReferenceDataRequest Class
#'
#' @field line_refs 
#' @field stop_point_refs 
#' @field date_utc 
#' @field mapping_version 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V3SiriReferenceDataRequest <- R6::R6Class(
  'V3SiriReferenceDataRequest',
  public = list(
    `line_refs` = NULL,
    `stop_point_refs` = NULL,
    `date_utc` = NULL,
    `mapping_version` = NULL,
    initialize = function(`line_refs`, `stop_point_refs`, `date_utc`, `mapping_version`){
      if (!missing(`line_refs`)) {
        stopifnot(is.list(`line_refs`), length(`line_refs`) != 0)
        lapply(`line_refs`, function(x) stopifnot(R6::is.R6(x)))
        self$`line_refs` <- `line_refs`
      }
      if (!missing(`stop_point_refs`)) {
        stopifnot(is.list(`stop_point_refs`), length(`stop_point_refs`) != 0)
        lapply(`stop_point_refs`, function(x) stopifnot(is.character(x)))
        self$`stop_point_refs` <- `stop_point_refs`
      }
      if (!missing(`date_utc`)) {
        stopifnot(is.character(`date_utc`), length(`date_utc`) == 1)
        self$`date_utc` <- `date_utc`
      }
      if (!missing(`mapping_version`)) {
        stopifnot(is.character(`mapping_version`), length(`mapping_version`) == 1)
        self$`mapping_version` <- `mapping_version`
      }
    },
    toJSON = function() {
      V3SiriReferenceDataRequestObject <- list()
      if (!is.null(self$`line_refs`)) {
        V3SiriReferenceDataRequestObject[['line_refs']] <- lapply(self$`line_refs`, function(x) x$toJSON())
      }
      if (!is.null(self$`stop_point_refs`)) {
        V3SiriReferenceDataRequestObject[['stop_point_refs']] <- self$`stop_point_refs`
      }
      if (!is.null(self$`date_utc`)) {
        V3SiriReferenceDataRequestObject[['date_utc']] <- self$`date_utc`
      }
      if (!is.null(self$`mapping_version`)) {
        V3SiriReferenceDataRequestObject[['mapping_version']] <- self$`mapping_version`
      }

      V3SiriReferenceDataRequestObject
    },
    fromJSON = function(V3SiriReferenceDataRequestJson) {
      V3SiriReferenceDataRequestObject <- jsonlite::fromJSON(V3SiriReferenceDataRequestJson)
      if (!is.null(V3SiriReferenceDataRequestObject$`line_refs`)) {
        self$`line_refs` <- lapply(V3SiriReferenceDataRequestObject$`line_refs`, function(x) {
          line_refsObject <- V3SiriLineRefDirectionRefStopPointRef$new()
          line_refsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          line_refsObject
        })
      }
      if (!is.null(V3SiriReferenceDataRequestObject$`stop_point_refs`)) {
        self$`stop_point_refs` <- V3SiriReferenceDataRequestObject$`stop_point_refs`
      }
      if (!is.null(V3SiriReferenceDataRequestObject$`date_utc`)) {
        self$`date_utc` <- V3SiriReferenceDataRequestObject$`date_utc`
      }
      if (!is.null(V3SiriReferenceDataRequestObject$`mapping_version`)) {
        self$`mapping_version` <- V3SiriReferenceDataRequestObject$`mapping_version`
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "line_refs": [%s],
           "stop_point_refs": [%s],
           "date_utc": %s,
           "mapping_version": %s
        }',
        lapply(self$`line_refs`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`stop_point_refs`, function(x) paste(paste0('"', x, '"'), sep=",")),
        self$`date_utc`,
        self$`mapping_version`
      )
    },
    fromJSONString = function(V3SiriReferenceDataRequestJson) {
      V3SiriReferenceDataRequestObject <- jsonlite::fromJSON(V3SiriReferenceDataRequestJson)
      self$`line_refs` <- lapply(V3SiriReferenceDataRequestObject$`line_refs`, function(x) V3SiriLineRefDirectionRefStopPointRef$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`stop_point_refs` <- V3SiriReferenceDataRequestObject$`stop_point_refs`
      self$`date_utc` <- V3SiriReferenceDataRequestObject$`date_utc`
      self$`mapping_version` <- V3SiriReferenceDataRequestObject$`mapping_version`
    }
  )
)
