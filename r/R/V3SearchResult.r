# PTV Timetable API - Version 3
#
# The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
#
# OpenAPI spec version: v3
# 
# Generated by: https://github.com/swagger-api/swagger-codegen.git

#' V3SearchResult Class
#'
#' @field stops 
#' @field routes 
#' @field outlets 
#' @field status 
#'
#' @importFrom R6 R6Class
#' @importFrom jsonlite fromJSON toJSON
#' @export
V3SearchResult <- R6::R6Class(
  'V3SearchResult',
  public = list(
    `stops` = NULL,
    `routes` = NULL,
    `outlets` = NULL,
    `status` = NULL,
    initialize = function(`stops`, `routes`, `outlets`, `status`){
      if (!missing(`stops`)) {
        stopifnot(is.list(`stops`), length(`stops`) != 0)
        lapply(`stops`, function(x) stopifnot(R6::is.R6(x)))
        self$`stops` <- `stops`
      }
      if (!missing(`routes`)) {
        stopifnot(is.list(`routes`), length(`routes`) != 0)
        lapply(`routes`, function(x) stopifnot(R6::is.R6(x)))
        self$`routes` <- `routes`
      }
      if (!missing(`outlets`)) {
        stopifnot(is.list(`outlets`), length(`outlets`) != 0)
        lapply(`outlets`, function(x) stopifnot(R6::is.R6(x)))
        self$`outlets` <- `outlets`
      }
      if (!missing(`status`)) {
        stopifnot(R6::is.R6(`status`))
        self$`status` <- `status`
      }
    },
    toJSON = function() {
      V3SearchResultObject <- list()
      if (!is.null(self$`stops`)) {
        V3SearchResultObject[['stops']] <- lapply(self$`stops`, function(x) x$toJSON())
      }
      if (!is.null(self$`routes`)) {
        V3SearchResultObject[['routes']] <- lapply(self$`routes`, function(x) x$toJSON())
      }
      if (!is.null(self$`outlets`)) {
        V3SearchResultObject[['outlets']] <- lapply(self$`outlets`, function(x) x$toJSON())
      }
      if (!is.null(self$`status`)) {
        V3SearchResultObject[['status']] <- self$`status`$toJSON()
      }

      V3SearchResultObject
    },
    fromJSON = function(V3SearchResultJson) {
      V3SearchResultObject <- jsonlite::fromJSON(V3SearchResultJson)
      if (!is.null(V3SearchResultObject$`stops`)) {
        self$`stops` <- lapply(V3SearchResultObject$`stops`, function(x) {
          stopsObject <- V3ResultStop$new()
          stopsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          stopsObject
        })
      }
      if (!is.null(V3SearchResultObject$`routes`)) {
        self$`routes` <- lapply(V3SearchResultObject$`routes`, function(x) {
          routesObject <- V3ResultRoute$new()
          routesObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          routesObject
        })
      }
      if (!is.null(V3SearchResultObject$`outlets`)) {
        self$`outlets` <- lapply(V3SearchResultObject$`outlets`, function(x) {
          outletsObject <- V3ResultOutlet$new()
          outletsObject$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE))
          outletsObject
        })
      }
      if (!is.null(V3SearchResultObject$`status`)) {
        statusObject <- V3Status$new()
        statusObject$fromJSON(jsonlite::toJSON(V3SearchResultObject$status, auto_unbox = TRUE))
        self$`status` <- statusObject
      }
    },
    toJSONString = function() {
       sprintf(
        '{
           "stops": [%s],
           "routes": [%s],
           "outlets": [%s],
           "status": %s
        }',
        lapply(self$`stops`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`routes`, function(x) paste(x$toJSON(), sep=",")),
        lapply(self$`outlets`, function(x) paste(x$toJSON(), sep=",")),
        self$`status`$toJSON()
      )
    },
    fromJSONString = function(V3SearchResultJson) {
      V3SearchResultObject <- jsonlite::fromJSON(V3SearchResultJson)
      self$`stops` <- lapply(V3SearchResultObject$`stops`, function(x) V3ResultStop$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`routes` <- lapply(V3SearchResultObject$`routes`, function(x) V3ResultRoute$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      self$`outlets` <- lapply(V3SearchResultObject$`outlets`, function(x) V3ResultOutlet$new()$fromJSON(jsonlite::toJSON(x, auto_unbox = TRUE)))
      V3StatusObject <- V3Status$new()
      self$`status` <- V3StatusObject$fromJSON(jsonlite::toJSON(V3SearchResultObject$status, auto_unbox = TRUE))
    }
  )
)
