/*
 * PTV Timetable API - Version 3
 * The PTV Timetable API provides direct access to Public Transport Victoria's public transport timetable data.    The API returns scheduled timetable, route and stop data for all metropolitan and regional train, tram and bus services in Victoria, including Night Network(Night Train and Night Tram data are included in metropolitan train and tram services data, respectively, whereas Night Bus is a separate route type).    The API also returns real-time data for metropolitan train, tram and bus services (where this data is made available to PTV), as well as disruption information, stop facility information, and access to myki ticket outlet data.    This Swagger is for Version 3 of the PTV Timetable API. By using this documentation you agree to comply with the licence and terms of service.    Train timetable data is updated daily, while the remaining data is updated weekly, taking into account any planned timetable changes (for example, due to holidays or planned disruptions). The PTV timetable API is the same API used by PTV for its apps. To access the most up to date data PTV has (including real-time data) you must use the API dynamically.    You can access the PTV Timetable API through a HTTP or HTTPS interface, as follows:        base URL / version number / API name / query string  The base URL is either:    *  http://timetableapi.ptv.vic.gov.au  or    *  https://timetableapi.ptv.vic.gov.au    The Swagger JSON file is available at http://timetableapi.ptv.vic.gov.au/swagger/docs/v3    Frequently asked questions are available on the PTV website at http://ptv.vic.gov.au/apifaq    Links to the following information are also provided on the PTV website at http://ptv.vic.gov.au/ptv-timetable-api/  * How to register for an API key and calculate a signature  * PTV Timetable API V2 to V3 Migration Guide  * PTV Timetable API Data Quality Statement    All information about how to use the API is in this documentation. PTV cannot provide technical support for the API.  
 *
 * OpenAPI spec version: v3
 *
 * NOTE: This class is auto generated by the swagger code generator program.
 * https://github.com/swagger-api/swagger-codegen.git
 *
 * Swagger Codegen version: 3.0.52
 *
 * Do not edit the class manually.
 *
 */
import {ApiClient} from '../ApiClient';
import {V3StopAccessibilityWheelchair} from './V3StopAccessibilityWheelchair';

/**
 * The V3StopAccessibility model module.
 * @module model/V3StopAccessibility
 * @version v3
 */
export class V3StopAccessibility {
  /**
   * Constructs a new <code>V3StopAccessibility</code>.
   * @alias module:model/V3StopAccessibility
   * @class
   */
  constructor() {
  }

  /**
   * Constructs a <code>V3StopAccessibility</code> from a plain JavaScript object, optionally creating a new instance.
   * Copies all relevant properties from <code>data</code> to <code>obj</code> if supplied or a new instance if not.
   * @param {Object} data The plain JavaScript object bearing properties of interest.
   * @param {module:model/V3StopAccessibility} obj Optional instance to populate.
   * @return {module:model/V3StopAccessibility} The populated <code>V3StopAccessibility</code> instance.
   */
  static constructFromObject(data, obj) {
    if (data) {
      obj = obj || new V3StopAccessibility();
      if (data.hasOwnProperty('lighting'))
        obj.lighting = ApiClient.convertToType(data['lighting'], 'Boolean');
      if (data.hasOwnProperty('platform_number'))
        obj.platformNumber = ApiClient.convertToType(data['platform_number'], 'Number');
      if (data.hasOwnProperty('audio_customer_information'))
        obj.audioCustomerInformation = ApiClient.convertToType(data['audio_customer_information'], 'Boolean');
      if (data.hasOwnProperty('escalator'))
        obj.escalator = ApiClient.convertToType(data['escalator'], 'Boolean');
      if (data.hasOwnProperty('hearing_loop'))
        obj.hearingLoop = ApiClient.convertToType(data['hearing_loop'], 'Boolean');
      if (data.hasOwnProperty('lift'))
        obj.lift = ApiClient.convertToType(data['lift'], 'Boolean');
      if (data.hasOwnProperty('stairs'))
        obj.stairs = ApiClient.convertToType(data['stairs'], 'Boolean');
      if (data.hasOwnProperty('stop_accessible'))
        obj.stopAccessible = ApiClient.convertToType(data['stop_accessible'], 'Boolean');
      if (data.hasOwnProperty('tactile_ground_surface_indicator'))
        obj.tactileGroundSurfaceIndicator = ApiClient.convertToType(data['tactile_ground_surface_indicator'], 'Boolean');
      if (data.hasOwnProperty('waiting_room'))
        obj.waitingRoom = ApiClient.convertToType(data['waiting_room'], 'Boolean');
      if (data.hasOwnProperty('wheelchair'))
        obj.wheelchair = V3StopAccessibilityWheelchair.constructFromObject(data['wheelchair']);
    }
    return obj;
  }
}

/**
 * Indicates if there is lighting at the stop
 * @member {Boolean} lighting
 */
V3StopAccessibility.prototype.lighting = undefined;

/**
 * Indicates the platform number for xivic information (Platform 0 indicates general stop facilities)
 * @member {Number} platformNumber
 */
V3StopAccessibility.prototype.platformNumber = undefined;

/**
 * Indicates if there is at least one audio customer information at the stop/platform
 * @member {Boolean} audioCustomerInformation
 */
V3StopAccessibility.prototype.audioCustomerInformation = undefined;

/**
 * Indicates if there is at least one accessible escalator at the stop/platform that complies with the Disability Standards for Accessible Public Transport under the Disability Discrimination Act (1992)
 * @member {Boolean} escalator
 */
V3StopAccessibility.prototype.escalator = undefined;

/**
 * Indicates if there is a hearing loop facility at the stop/platform
 * @member {Boolean} hearingLoop
 */
V3StopAccessibility.prototype.hearingLoop = undefined;

/**
 * Indicates if there is an elevator at the stop/platform
 * @member {Boolean} lift
 */
V3StopAccessibility.prototype.lift = undefined;

/**
 * Indicates if there are stairs available in the stop
 * @member {Boolean} stairs
 */
V3StopAccessibility.prototype.stairs = undefined;

/**
 * Indicates if the stop is accessible
 * @member {Boolean} stopAccessible
 */
V3StopAccessibility.prototype.stopAccessible = undefined;

/**
 * Indicates if there are tactile tiles (also known as tactile ground surface indicators, or TGSIs) at the stop
 * @member {Boolean} tactileGroundSurfaceIndicator
 */
V3StopAccessibility.prototype.tactileGroundSurfaceIndicator = undefined;

/**
 * Indicates if there is a general waiting area at the stop
 * @member {Boolean} waitingRoom
 */
V3StopAccessibility.prototype.waitingRoom = undefined;

/**
 * @member {module:model/V3StopAccessibilityWheelchair} wheelchair
 */
V3StopAccessibility.prototype.wheelchair = undefined;

